1.java中函数重载，可以通过参数的个数和参数的类型来实现，不能通过返回值的类型；
2.this是当前对象的句柄，通常作为返回值return，或者在构造函数中使用，但要在第一行就用，当形参名和类中变量相同时也用this来指定类中的变量。static方法没有this。
3.finalize()函数用于释放那些非Java使用的内存

1.初始化过程中static修饰的最先初始化且只初始化一次， 构造函数里的最后初始化。
2.数组的初始化方式 


1.需要上溯造型时用继承，不需要时可以用合成。
2.

一个复杂的对象，构建器的调用遵照下面的顺序：
(1) 调用基础类构建器。这个步骤会不断重复下去，首先得到构建的是分级结构的根部，然后是下一个衍生
类，等等。直到抵达最深一层的衍生类。
(2) 按声明顺序调用成员初始化模块。
(3) 调用衍生构建器的主体。						